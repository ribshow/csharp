# MongoDB com ASPNET

**Crie a string de conexão**

- "MongoConnection": {
    "ConnectionString": "mongodb://localhost:27017",
    "Database": "asp_mongo",
    "IsSSL": true
  }

**Instale os pacotes necessários**
- MongoDB.Driver

**Crie o context do banco de dados**
**Registre esse context no program**
